#include<bits/stdc++.h>
using namespace std;
int main () {
    vector<int>nums = {1,2,4,5};
    for(int x:nums){
        cout<<x<<endl;
    }
}

// This whole is the shorthand for the range based loop where it runs
// for rach value of the x and print it into the terminal .
