#include <bits/stdc++.h>
using namespace std; 
class Node {
    public:
    int data;
    Node* next;

   Node(int x) {
    data =x;
    next = nullptr;
   };

};

// Function to insert the data in front .

Node* insertAtFont(Node* head,int x) {
 
    Node* newNode = new Node(x);

    

}