# Dynamic Array 
- A dynamic array is an array whose size can changed during the execution of the program . The most popular dynamic array in c++ is the vector structure, which can be used almost like and ordinary array .
- The function size return the number of the elements in the vector . 
- 